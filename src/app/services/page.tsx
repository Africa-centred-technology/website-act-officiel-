import ServicesShellClient from "@/components/services/ServicesShellClient";

export const metadata = {
  title: "Nos Services — Africa Centred Technology",
  description:
    "Ingénierie logicielle, IA agentique, architecture cloud, data, géomatique, conseil stratégique et formation — l'arborescence complète des expertises ACT.",
};

export default function ServicesPage() {
  return <ServicesShellClient />;
}
